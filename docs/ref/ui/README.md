# UI reference

`design_ref.zip` is the attached visual baseline. Before frontend implementation, extract it into a temporary workspace, inventory all routes/screens/components/assets, and update `docs/product/ui-screen-inventory.md` and `ui-backend-capability-map.md` with any newly discovered behavior.

Do not serve or commit generated extraction directories unless they are required assets. Preserve the original archive for comparison. Treat visible sample data as fixtures, not production requirements or secrets.
