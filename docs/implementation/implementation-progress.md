# Groundloom implementation progress

This file is the live execution record for the autonomous implementation. It is updated with each validated vertical slice; a phase is only marked complete when its documented exit gate has evidence.

## Scope and governing requirements

The implementation covers FR-PROJECT-001..003, FR-SOURCE-001..003, FR-SKILL-001..002, FR-AGENT-001..002, FR-CONTENT-001..005, FR-QUALITY-001, FR-EXPORT-001, FR-AUDIT-001, UI-STATE-001..002, UI-RUN-001..002, UI-PATCH-001..002, UI-CITE-001, UI-A11Y-001, the NFR reliability/performance/security/cost targets, and the ARCH/DATA/SEC/API/EVT/TOOL invariants in the normative documents.

Non-goals remain general web crawling, real-time CRDT collaboration, arbitrary shell access, autonomous publication, model training, and unrelated workflow automation.

## Execution status

| Phase | Status | Evidence / next action |
|---|---|---|
| 00 Repository and contracts | completed-local | Backend tests, ruff, mypy, docs validator, Vite build, and browser smoke passed on 2026-08-24. |
| 01 Domain and persistence | completed-local | Tenant-safe SQLAlchemy domain, lifecycle, runs/events/outbox/audit/content foundations, role-scoped cursor-paginated audit reads, transaction-scoped tenant context, migration 014 project-key repair, API vertical tests, and disposable live Postgres/RLS role evidence passed; previous-release migration rehearsal remains a release gate. |
| 02 Source ingestion and retrieval | completed-local | Immutable PDF/DOCX/TXT/MD upload and revision lineage, parsing, explicit scanned-PDF OCR stage with local refusal and HTTP sidecar adapter, normalized blocks/chunks, scoped hybrid lexical/semantic retrieval, deterministic local/OpenAI-compatible embedding adapters, automatic Postgres/pgvector derived-index storage, passage navigation, prompt-injection signals, durable leased jobs, explicit idempotent derived-index rebuild, idempotent replay, and worker entrypoints are implemented and tested locally; production ANN/reranker/OCR and golden-corpus evidence remain external. |
| 03 Primary agent foundation | completed-local | One persistent project thread, adaptive deterministic local runtime, bounded local execution checkpoints, local direct/delegated trajectory tests, durable todos/events, checkpoint seam, SSE replay, cancellation, pinned optional Deep Agents/Postgres adapter with verified message/update stream projection, provider safety boundary, approval interrupts, redacted run usage/budget metadata, and leased staging/production agent-worker dispatch are implemented; live provider/recovery evidence is deployment work. |
| 04 Skills, memory, and subagents | completed-local | Versioned skill validation/publication, draft-only skill-author endpoint, scoped redacted memory, bounded delegated-task records with retry/reconciliation, a leased delegated-task worker, and proposal-only tool registry are implemented and tested. |
| 05 Content generation and review | completed-local | Typed immutable content/outline versions, cited proposals, deterministic validation, Accept/Reject, optimistic conflict handling, provenance, durable plan approval, same-thread continuation, and rejection safety are implemented and tested. |
| 06 Quality, evaluation, and export | completed-local | Deterministic validators, versioned rubric/baseline runner, redacted evaluation observation adapter, bounded OpenAI-compatible semantic evaluator adapter, validation findings, idempotent PDF/DOCX/Markdown/HTML export, and preview/download are implemented; export now has a durable leased worker with explicit local inline mode; live evaluator quality baselines and telemetry feedback promotion remain external-provider work. |
| 07 Frontend integration | completed-local | Reference-informed React/Vite UI uses a shared API client, bounded cursor-paginated project loading, durable SSE replay/reconnect with connection state, connected typed workspace Settings persistence, active published-skill selection, skill scope filtering/fork/author/repair/publish controls, immutable source-version history/revision upload, exact citation lookup, deterministic validation checklist, typed in-app retry/permission/terminal notices with no native alerts, native API/UI contract tests (5 passed), actual React component-rendering tests (3 passed), 10 Playwright tests (9 semantic journeys plus the visual-baseline test), and committed Windows Chromium empty-state baselines; browser coverage includes project creation, approval resume, proposal accept/reject, permission-denied rendering, settings, command palette, source version history/revision, source upload/evidence selection, citation navigation, skill lifecycle, validation review, and dropped-stream reconnect. |
| 08 Security, reliability, and operations | completed-local | Signed runtime identity, parser/path safety, tenant/tool-scope tests, bounded retries, leased-worker replay, checksum-backed local backup/restore, synthetic retrieval benchmark, redaction, alerts, role-bound RLS policy generation, dedicated worker/migrator runtime roles, and CI checks exist; disposable live Postgres/RLS/checkpoint/S3 integration passed. Production restore/load, sandbox, and incident exercises remain explicit deployment evidence. |
| 09 Production readiness | blocked-external-evidence | Local release is runnable and validated. Production sign-off still requires installed/pinned Deep Agents, Postgres/pgvector, object storage, identity, telemetry, backup/restore, load, and rollback evidence. |

## Initial requirement-to-slice map

| Slice | Requirements | Planned proof |
|---|---|---|
| Contract/runtime foundation | API-*, EVT-*, SEC-AUTH-001..006, NFR-REL-001..004 | OpenAPI/schema tests, config safety tests, health/error tests, docs validation |
| Tenant-safe domain | FR-PROJECT-001..003, FR-AUDIT-001, DATA-*, OPS-* | Repository/service integration and cross-tenant tests |
| Sources/retrieval | FR-SOURCE-001..003, TOOL-RET-001..002 | Parser, lineage, scope, replay, and retrieval tests |
| Agent harness | FR-AGENT-001..002, TOOL-*, AGENT-* | trajectory, interruption, replay, prohibited-call tests |
| Content/review | FR-CONTENT-001..005 | deterministic proposal/accept/reject/conflict tests |
| Quality/export | FR-QUALITY-001, FR-EXPORT-001 | validator, artifact idempotency, download/auth tests |
| UI | UI-* and acceptance journeys J1..J6 | component, accessibility, reconnect, and browser tests |
| Hardening/release | NFR-*, SEC-*, OPS-* | security, reliability, load, migration, backup/restore evidence |

## Decisions and deviations

- Use a modular FastAPI/SQLAlchemy monolith with Postgres-compatible schemas and a SQLite local/test adapter so the application remains runnable without production credentials. Production mode refuses SQLite and in-memory checkpoint/object storage.
- Use a deterministic local model adapter by default for tests/local development; provider adapters are configured explicitly and never pretend to be production success.
- Configure the S3-compatible adapter with explicit connect/read timeouts and bounded SDK retries; all provider failures are mapped to redacted typed dependency errors.
- Use PDF/DOCX/TXT source types and PDF/DOCX exports per the accepted defaults in `assumptions-risks-open-questions.md`.
- The UI reference extraction is temporary and excluded from version control; the archive remains the comparison source.

## Evidence log

| Date | Phase/slice | Evidence |
|---|---|---|
| 2026-08-24 | Establish source of truth | Read root contract, README/docs index, product, architecture, roadmap, dependency map, checklist, release gates, governance risks, phase documents, contracts, components, Deep Agents specifications, and UI archive inventory. |
| 2026-08-25 | Provider/jobs/evaluation hardening | `python -m pytest -q` passed (18 tests); Ruff and mypy passed; deterministic evaluation runner passed; durable ingestion lease/replay, path-safe object storage, production configuration, and optional provider adapter tests passed. |
| 2026-08-25 | Security/reliability hardening | `python -m pytest -q` passed (27 tests plus one optional-provider skip); signed identity rejects missing/tampered bearer context and unsafe production configuration; configured provider failure is retryable and never local success; MIME-spoofed DOCX failure persists; local backup manifest verifies; `benchmark_local.py --requests 20` reported a synthetic p95; parser, database, and benchmark cleanup paths were exercised. |
| 2026-08-25 | Runtime identity and UI reconnect hardening | Staging/production contexts now require signed bearer identity; local frontend SSE replay reconnects with `Last-Event-ID` and displays connection state; frontend build and backend gates remain green. |
| 2026-08-25 | Isolated provider adapter verification | Optional extras installed cleanly in a disposable venv; fake-model Deep Agents graph compile passed for a bounded specialist subagent and the Groundloom runtime factory compiled its scoped tool/subagent harness; Langfuse adapter outage probe remained bounded and did not touch product state; live Postgres/S3/provider credentials remain unavailable. |
| 2026-08-25 | Export, retention, derived-index, and delegation worker slice | Added migrations 003-005, durable export leases/worker, project deletion request/status API, legal-hold policy seam, scoped unshared source/object/checkpoint cleanup, explicit derived lexical-index rebuild API/worker, queued-export polling, leased delegated-task retry consumption, audit/outbox completion, and focused worker tests; the subsequent full backend suite passed 32 tests with one optional-provider skip, frontend API tests 3 passed, frontend build passed, and docs validation passed for 166 documents. |
| 2026-08-25 | Approval and usage slice | Added migration 006, durable plan approval records and events, scoped approval resolution with same-thread continuation/rejection safety, and redacted per-run usage/budget metadata; focused approval test passed. |
| 2026-08-25 | Workspace preferences slice | Added migration 007, typed/audited/idempotent workspace preferences, project pinning with explicit-default precedence, connected Settings UI, and API coverage for persistence/replay/audit-facing behavior. |
| 2026-08-25 | Agent worker slice | Added migration 008, durable AgentRun leases/attempts, staging/production queue dispatch, a real `agent_worker.py` loop, and local worker-mode coverage while preserving inline deterministic development execution. |
| 2026-08-25 | Operational controls slice | Added migrations 009-010, worker heartbeats and bounded readiness/health snapshots, workspace daily token/cost budgets with durable waiting state, provider outage redaction/classification, and S3/Langfuse adapter failure tests; full backend suite passed 38 tests with one optional-provider skip, Ruff/mypy/migration verification passed. |
| 2026-08-25 | Storage adapter hardening | Added explicit S3 connect/read timeout and bounded retry configuration; put/get/head/delete failures now share the typed dependency boundary and focused adapter tests cover write/delete redaction. |
| 2026-08-25 | Durable run controls | Connected the canvas Copilot panel to durable run status plus cancel/resume commands; added staging-shaped cancellation replay coverage and refreshed the browser/API traceability evidence. |
| 2026-08-25 | Final local audit | Deterministic eval reported 1 pass plus 1 intentional needs-revision case; local retrieval benchmark reported p50 6.082 ms/p95 9.316 ms over 20 requests; migrations 001-012 verified and the working tree remains clean after checkpointing. |
| 2026-08-25 | PostgreSQL tenant isolation slice | Added migrations 011 and 013 for forced RLS policies over workspace-owned tables and a dedicated `groundloom_worker` database-role boundary, plus transaction-local context propagation across API commits; ADR-024, security architecture, configuration contract, and traceability were updated. Live role grants/policy execution remain a release gate. |
| 2026-08-25 | Primary-thread serialization slice | Enforced one active mutation turn per project with idempotent same-key replay, a partial unique index, and typed 409 conflict guidance for concurrent requests; added API coverage and updated the run/event contract. |
| 2026-08-25 | Browser E2E slice | Added Playwright web-server orchestration with isolated SQLite/object-store adapters and axe scanning; 3 browser tests passed for project → collaborator → proposal → accept, settings persistence/command-palette navigation, and serious/critical accessibility violations. Visual baselines remain intentionally environment-specific. |
| 2026-08-25 | Frontend supply-chain hardening | Upgraded Vite to the Node-18-compatible patched 6.4.3 line; full `npm audit --audit-level=high` and production-only audit both report zero vulnerabilities. |
| 2026-08-25 | Current local release audit | Backend 51 tests passed with two optional-provider skips and three deployment-integration skips in the default environment; the isolated pinned-provider probe passed both optional tests. Frontend native 5, component 3, and Playwright 10 tests passed on the pinned Windows lane, including active skill selection, source version history/revision upload, exact citation navigation, skill scope filtering/forking, skill repair/publication, approval resume, dropped-stream reconnect, permission-denied rendering, visual baselines, and canonical rejection safety; CI-mode Playwright passed the 9 semantic tests with only the Windows pixel test skipped. Build, Ruff, mypy, migration verification, docs validation, compose validation, and npm audits passed. Runtime workers now use the dedicated worker DB URL, production API startup skips schema bootstrap, migrations initialize checkpoint schema through the migrator role, staging/production role separation is validated, and the local role-boundary regression tests pass. Retrieval benchmark reported p50 6.641 ms/p95 9.791 ms over 20 requests. `pip check` remains red only for the unrelated global Streamlit/protobuf conflict. |
| 2026-08-25 | Deployment-shaped database/storage integration | Docker Compose Postgres/pgvector and MinIO started with local-only role/bucket bootstrap; migrations through 015 passed through the migrator role, LangGraph checkpoint tables and the pgvector derived-index table were initialized, and `backend/tests/test_postgres_deployment_integration.py` passed 4 tests for API/worker RLS visibility, forced policies, checkpoint schema, scoped pgvector search, and S3-compatible put/get/delete. This closes disposable infrastructure evidence; production provider, restore/load, identity, telemetry, and release-owner gates remain external. |
| 2026-08-25 | Frontend completion and skills/source fidelity slice | Added immutable `PUT /v1/skill-versions/{id}/repair`, idempotent published-skill forking, active skill selection during project creation, scope filters, source-version history/revision upload, exact immutable citation lookup, connected AI author/validate/repair/publish UI controls, fixed current-run selection after sending a message, added typed permission/retryable/terminal error states, and retained 10 Playwright tests plus two committed Windows Chromium empty-state baselines. Focused skill, source-history/citation, native, component, build, lint, and type gates passed; the complete pinned Windows browser suite passed 10/10 after a deterministic reconnect assertion fix. |
| 2026-08-25 | Checkpoint concurrency hardening | Replaced the shared local checkpoint temp filename with unique sibling files and a bounded in-process atomic-write lock; the concurrent writer regression passes and prevents Windows retry/worker initialization collisions. |
| 2026-08-25 | Typed frontend failure-state slice | Replaced remaining native browser alerts in source upload, canvas operations/export/review, project creation, and settings with accessible typed in-app notices supporting retry or dismissal; the UI contract gate now rejects `alert(` regressions and native/component/build gates pass. |
| 2026-08-25 | Cursor pagination slice | Added bounded workspace-scoped `GET /v1/projects/page` keyset pagination with opaque cursors and invalid-cursor errors; Projects now loads the real page contract and exposes an accessible Load more state. Backend cursor/ordering/scope tests, frontend contract/build gates passed. |
| 2026-08-25 | Hybrid retrieval adapter slice | Added deterministic fixed-dimension local embeddings, an OpenAI-compatible embedding adapter with typed outage/malformed-response handling, embedding persistence during ingestion/index rebuild, and bounded hybrid lexical/semantic evidence scoring. Focused retrieval/index/provider tests passed; production pgvector/reranker and golden quality evidence remain external. |
| 2026-08-25 | Transactional outbox worker slice | Added an at-least-once webhook delivery adapter, explicit disabled-sink refusal, persisted delivery attempts, retryable redacted outage handling, worker heartbeat, and `backend/scripts/outbox_worker.py`; replay/failure tests passed. |
| 2026-08-25 | Audit query boundary slice | Added the admin-only `GET /v1/audit-events` safe projection with bounded keyset pagination, invalid-cursor handling, workspace isolation, and post-read `audit.read` recording; focused API test passed and documentation/traceability were updated. |
| 2026-08-25 | Deployment vector-index slice | Added migration 015, pgvector-derived storage/search with scoped dimensions and typed outage handling, automatic SQLite/PostgreSQL backend selection, RLS coverage, and deployment integration assertions; local vector-store tests and migration verification passed. |
| 2026-08-25 | Retrieval quality pipeline slice | Added deterministic/Cohere-compatible reranker adapters, bounded `hybrid.v2` scoring, same-version neighbor expansion, normalized duplicate suppression, and pinned retrieval configuration/provenance; focused pipeline tests and the full backend suite passed with 66 tests. |
| 2026-08-25 | Semantic evaluation provider slice | Added a bounded OpenAI-compatible structured semantic grader behind the existing rubric protocol, explicit score/verdict/feedback validation, typed outage/misconfiguration handling, and evaluator configuration pinning; the full backend suite passed with 69 tests. |
| 2026-08-25 | Semantic review browser evidence | Updated the reference review journey to assert citation-required semantic `Needs revision` findings for source-less drafts; the full pinned Windows Playwright suite passed 10/10 after the contract update, with native frontend 5, component 3, and production build gates also passing. |
| 2026-08-25 | Artifact authorization and shared-retention slice | Added short-lived HMAC download capabilities scoped to user/workspace/export ID with active-membership and expiry checks plus download auditing; corrected project deletion to preserve source bytes shared by another project. Full backend suite passed 71 tests, Ruff/mypy/docs validation passed, and signed-capability/shared-artifact regressions passed. |
| 2026-08-25 | Untrusted rendering and telemetry redaction slice | Escaped untrusted HTML/DOCX export text, added malicious-markup regression coverage, and made telemetry redaction recursive across nested maps/lists with authorization/token key coverage; full backend suite passed 72 tests and Ruff/mypy remained green. |
| 2026-08-25 | Source safety and quarantine slice | Added deterministic local source scanning, an HTTP scanner-sidecar adapter with typed outage/malformed verdict handling, terminal `quarantined` source state for unsafe fixtures/active document features, bounded multipart upload reads, and worker preservation of quarantine after replay; the then-current backend suite passed 78 tests, Ruff/mypy/docs validation remained green, frontend native/component/build gates and the 10-test Playwright suite passed, and production configuration rejects the local scanner. |
| 2026-08-25 | Local restore safety slice | Made backup restore validate the source manifest and exact object inventory before touching the target, verify the restored copy afterward, and reject corrupt/extra-object backups without overwriting an existing target; focused recovery regression passed. |
| 2026-08-25 | Typed content-block contract slice | Added deterministic supported-block and payload-shape validation for headings, text, procedures/lists, tables, figures, quizzes/checklists, and source lists at proposal and acceptance boundaries; malformed/unknown block regression coverage plus one valid/invalid case per supported type passed, and the canvas now renders the typed families through the real content API. The full backend suite is now 98 passed / 104 collected with six intentional default-environment skips. |
| 2026-08-25 | Final typed-content release gate | Full backend suite passed 98 tests with six intentional default-environment skips; optional provider probe remains 2/2, disposable Postgres/RLS/pgvector/checkpoint/S3 integration remains 4/4, Ruff, mypy (38 files), migrations, documentation validation (175 documents), deterministic evaluations, native frontend 5, component 3, production build, and pinned Windows Playwright 10/10 all passed. Retrieval benchmark reported p50 11.766 ms/p95 17.050 ms over 20 requests. |
| 2026-08-25 | OCR adapter slice | Added explicit scanned-PDF `ocr` ingestion stage, bounded HTTP OCR provider, typed local non-success behavior, production configuration guard, OCR outage/malformed response tests, and end-to-end stage persistence. The then-current backend suite passed 101 tests with six intentional default-environment skips; mypy covered 39 source files and documentation validation covered 176 markdown documents. |
| 2026-08-25 | Storage encryption boundary | Added explicit `object_store_sse_mode`/KMS configuration, production startup refusal for unencrypted S3-compatible storage, AES-256/KMS write headers, and adapter/configuration regression coverage; local and MinIO development remain unencrypted only by explicit local configuration. |
| 2026-08-25 | Current local release gate | Full backend suite passed 104 tests (111 collected; seven intentional default-environment skips), Ruff, mypy (39 source files), migrations 001-015, documentation validation (176 documents), native frontend 5, component frontend 3, pinned Windows Playwright 10, Vite production build, deterministic evaluation, retrieval benchmark, and deployment-shaped Postgres/RLS/checkpoint/pgvector/S3 integration passed; Phase 08 is now marked completed-local and Phase 09 remains blocked only on external production evidence. |
| 2026-08-25 | Provider stream projection slice | Configured Deep Agents execution now consumes the verified LangGraph `messages`/`updates` stream, persists bounded provider/tool/subagent progress through the durable SSE/outbox path, applies the run budget as a recursion limit, and observes worker cancellation between chunks; pure stream normalization/cancellation tests passed, while live provider execution remains external. |
| 2026-08-25 | Bidirectional traceability gate | Strengthened `validate_docs.py` to derive every normative FR/NFR/SEC/UI identifier from the product documents and verify exact or grouped-range coverage in the requirements matrix; the validator passed across 176 markdown documents. |
